version: '3.8'
services:
  frontend:
    build: .
    ports:
      - "5173:80"
    depends_on:
      - backend
    environment:
      - NODE_ENV=production

  backend:
    build: ./server
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
